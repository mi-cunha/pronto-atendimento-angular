import { AtendimentoConcluidoDirective } from './atendimento-concluido.directive';

describe('AtendimentoConcluidoDirective', () => {
  it('should create an instance', () => {
    const directive = new AtendimentoConcluidoDirective();
    expect(directive).toBeTruthy();
  });
});
